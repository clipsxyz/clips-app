<?xml version="1.0" encoding="utf-8"?>
<!-- Allow cleartext HTTP to local Laravel during development (LAN / emulator / adb reverse). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
